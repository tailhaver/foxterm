[project]
name = "foxes-new"
version = "0.2.0"
description = "taggie waggie's website"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "quart>=0.20.0",
    "quart-cors>=0.8.0",
]
